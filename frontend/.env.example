# ── API Base URL ───────────────────────────────────────────────
# Leave empty in development (Vite proxy handles /api → localhost:3001)
# Set to your Render backend URL in production, e.g.:
# VITE_API_URL=https://agentiq-backend.onrender.com
VITE_API_URL=
